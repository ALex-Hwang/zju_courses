#include "func_cli.h"

void getUserName(char* userName) {
    printf("The user name of the server is: ");
    printf("%s\n", userName);
}

void printTime(char* Time) {
    printf("Corrent time on the server is: ");
    printf("%s\n", Time);
}

