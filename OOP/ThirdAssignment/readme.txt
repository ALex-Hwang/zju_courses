几点说明：
1. 本程序的房间并不是一开始就设定好的，而是根据每次用户的输入来决定是否新建房间。
2. 由于实现方式所限，本游戏的一二楼的房间是没有关联的，也就是说，其是两个独立的楼层空间，从哪上来就必须从哪里下去。
3. 本游戏并未对输入数据进行检查，且对于输入大小写敏感。
4. 压缩包中a.out为linux下可执行文件。
5. 同样由于实现方式所限，data.txt中的测试数据仅供参考，还是需要用户自行在终端中选择路径。